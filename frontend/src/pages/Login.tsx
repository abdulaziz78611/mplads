import { useState } from 'react'
import { ArrowRight, Eye, EyeOff, LockKeyhole, Network, ShieldCheck } from 'lucide-react'
import { useNavigate } from 'react-router-dom'
import { api } from '../services/api'
export default function Login({ onLogin }: { onLogin: (user: any) => void }) { const navigate=useNavigate(); const [email,setEmail]=useState('officer@mplad.demo');const [password,setPassword]=useState('Demo@123');const [show,setShow]=useState(false);const [error,setError]=useState('');const [loading,setLoading]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setLoading(true);setError('');try{const result=await api.login(email,password);localStorage.setItem('mplad_token',result.access_token);localStorage.setItem('mplad_user',JSON.stringify(result.user));onLogin(result.user);navigate('/dashboard')}catch(err:any){setError(err.message)}finally{setLoading(false)}};return <div className="login-page"><div className="login-panel"><div className="login-brand"><div className="brand-mark"><Network size={25}/></div><div><b>MPLAD Sentinel</b><span>Anomaly Monitoring Platform</span></div></div><div className="login-copy"><div className="eyebrow">AUTHORISED ACCESS</div><h1>Decision support for accountable public works.</h1><p>Detect unusual patterns, organise evidence and prioritise human-led review across MPLAD projects.</p></div><div className="login-safeguards"><span><ShieldCheck size={18}/> Explainable anomaly signals</span><span><ShieldCheck size={18}/> Auditable review workflow</span><span><ShieldCheck size={18}/> Human decision remains essential</span></div></div><section className="login-card"><div><div className="login-lock"><LockKeyhole size={22}/></div><h2>Sign in</h2><p>Use the demonstration officer account to explore the prototype.</p></div><form onSubmit={submit}><label>Official email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<div className="password-field"><input type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} required/><button type="button" onClick={()=>setShow(!show)} aria-label="Show password">{show?<EyeOff size={17}/>:<Eye size={17}/>}</button></div></label>{error&&<div className="login-error">{error}</div>}<button className="btn primary login-button" disabled={loading}>{loading?'Signing in…':<>Sign in securely <ArrowRight size={17}/></>}</button></form><div className="demo-credentials"><b>Demo credentials</b><span>officer@mplad.demo · Demo@123</span></div><small className="login-footer">Demonstration dataset — synthetic data for prototype evaluation.</small></section></div> }
